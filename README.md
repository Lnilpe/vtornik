# Отчёт по ревью 
ФИО: Ревягин Фёдор Дмитриевич
Группа: 35ИС
Программист: Ревягин Фёдор Дмитриевич
Ревьюер: Ревягин Фёдор Дмитриевич
Вариант: 1
## 1. Что требует ТЗ: Возраст 18+ -> True, младше 18 -> False.
## 2. Что код делает на самом деле: Проверяет только age > 18. Если больше 18 - строка "да". Если 18 или м
## 3. Несоответствия: Нужны True/False, возврощается текст "да"
## 4. Вердикт: принять / принять с замечаниями / отклонить: отклонить 
## 5. Что исправить: True/False

Код на ревью
```py
def can_buy(age)
    if age > 18:
        return "да"
```
Готовый код
```py
def can_buy(age):
    if age >= 18:
        return True
    return False
```
# Вариант 2
## 1. Что требует ТЗ: Чтобы пароль был больше 8 символов иначе пароль слабый
## 2. Что код делает на самом деле: Проверяет длину пароля
## 3. Несоответствия: else
## 4. Вердикт: принять / принять с замечаниями / отклонить: отклонить
## 5. Что исправить: добавить
Код на ревью
```py
def is_strong(password):
    if len(password) > 8:
        return True
```
Готовый код
```py
def is_strong(password):
    return len(password) >= 8

password = input("Введите пароль: ")
print("Надёжный пароль!" if is_strong(password) else "Слабый пароль!")
```
# Вариант 3
## 1. Что требует ТЗ: Если цена 1000 и больше - вернуть цену со сидкой 10%
## 2. Что код делает на самом деле: Если цена больше 1000 то скидка 10%
## 3. Несоответствия:
## 4. Вердикт: принять / принять с замечаниями / отклонить: отклонить
## 5. Что исправить:
Код на ревью
```py
def final_price(price):
    if price > 1000:
        return price - 10
```
Готовый код
```py
def final_price(price):
    if price >= 1000:
        return price - price * 0.1
    return price
```
# Вариант 4
## 1. Что требует ТЗ: 
## 2. Что код делает на самом деле: 
## 3. Несоответствия:
## 4. Вердикт: принять / принять с замечаниями / отклонить:
## 5. Что исправить:
Код на ревью
```py
def is_pass(score):
    if score >= 60:
        print("зачёт")
```
Готовый код
```py
def is_pass(score):
    return score >= 60

score = int(input("Введите баллы: "))
print("Зачёт!" if is_pass(score) else "Не зачёт!")
```
# Вариант 5
## 1. Что требует ТЗ: 
## 2. Что код делает на самом деле: 
## 3. Несоответствия:
## 4. Вердикт: принять / принять с замечаниями / отклонить:
## 5. Что исправить:
Код на ревью
```py
def need_jacket(temp):
    if temp < 10:
        return False
```
Готовый код
```py
def need_jacket(temp):
    return temp < 10

temp = float(input("Введите температуру: "))
print("Нужна куртка!" if need_jacket(temp) else "Куртка не нужна!")
```