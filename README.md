# Отчёт по ревью 
# ФИО: Ревягин Фёдор Дмитриевич
# Группа: 35ИС
# Программист: Ревягин Фёдор Дмитриевич
# Ревьюер: Ревягин Фёдор Дмитриевич
# Вариант: 1
## 1. Что требует ТЗ: Возраст 18+ -> True, младше 18 -> False.
## 2. Что код делает на самом деле: Проверяет только age > 18. Если больше 18 - строка "да". Если 18 или м
## 3. Несоответствия: Нужны True/False, возврощается текст "да"
## 4. Вердикт: принять / принять с замечаниями / отклонить: отклонить 
## 5. Что исправить: True/False

Код на ревью
```py
def can_buy(age)
    if age > 18:
        return "да"
```
Готовый код
```py
def can_buy(age):
    if age >= 18:
        return True
    return False
```
# Вариант 2
## 1. Что требует ТЗ: Чтобы пароль был больше 8 символов иначе пароль слабый
## 2. Что код делает на самом деле: Проверяет длину пароля
## 3. Несоответствия: else
## 4. Вердикт: принять / принять с замечаниями / отклонить: отклонить
## 5. Что исправить: добавить
Код на ревью
```py
def is_strong(password):
    if len(password) > 8:
        return True
```
Готовый код
```py
def is_strong(password):
    return len(password) >= 8

password = input("Введите пароль: ")
print("Надёжный пароль!" if is_strong(password) else "Слабый пароль!")
```
# Вариант 3
## 1. Что требует ТЗ: Если цена 1000 и больше - вернуть цену со сидкой 10%
## 2. Что код делает на самом деле: Если цена больше 1000 то скидка 10%
## 3. Несоответствия:
## 4. Вердикт: принять / принять с замечаниями / отклонить: отклонить
## 5. Что исправить:
Код на ревью
```py
def final_price(price):
    if price > 1000:
        return price - 10
```
Готовый код
```py
def final_price(price):
    if price >= 1000:
        return price - price * 0.1
    return price
```
# Вариант 4
## 1. Что требует ТЗ: Баллы 60+ → True, меньше 60 → False.
## 2. Что код делает на самом деле: Проверяет только score >= 60. Если баллов 60 и больше — выводит текст «зачёт». Если меньше 60 — ничего не возвращает.
## 3. Несоответствия: В ТЗ нужно возвращать True/False, а код выводит текст «зачёт».Нет return False для баллов меньше 60.Нет проверки, что score — число.Код не возвращает никакого значения (отсутствует return).
## 4. Вердикт: принять / принять с замечаниями / отклонить: отклонить
## 5. Что исправить: Возвращать True/False вместо печати текста.Добавить return False для баллов меньше 60.
Код на ревью
```py
def is_pass(score):
    if score >= 60:
        print("зачёт")
```
Готовый код
```py
def is_pass(score):
    return score >= 60

score = int(input("Введите баллы: "))
print("Зачёт!" if is_pass(score) else "Не зачёт!")
```
# Вариант 5
## 1. Что требует ТЗ: Температура < 10 → True, температура ≥ 10 → False.
## 2. Что код делает на самом деле: Проверяет только temp < 10. Если температура ниже 10 — возвращает False. Если 10 и выше — ничего не возвращает.
## 3. Несоответствия: В ТЗ нужно возвращать True при температуре ниже 10, а код возвращает False.
## 4. Вердикт: принять / принять с замечаниями / отклонить: отклонить
## 5. Что исправить: Возвращать True при temp < 10
Код на ревью
```py
def need_jacket(temp):
    if temp < 10:
        return False
```
Готовый код
```py
def need_jacket(temp):
    return temp < 10

temp = float(input("Введите температуру: "))
print("Нужна куртка!" if need_jacket(temp) else "Куртка не нужна!")
```



# A1 
# Код-ревью функции make_phone
## 1. Несоответствия: Обработка букв: В коде проверяется только наличие буквы "a", но не все буквы. ТЗ требует, чтобы все буквы были запрещены.
## 2. Вердикт: принять / принять с замечаниями / отклонить: отклонить
## 3. Что исправить: Проверять, что строка состоит только из цифр использовать .isdigit()
Код на ревью
```py
def make_phone(raw):
    try:
        text = str(raw)
        if "a" in text or text:
            return None
        number = "+" + text
        if len(number) > 11:
            return None
        return number
    except:
        print("ошибка")
    finally:
        print("проверка номера завершена")
        return number
```
Готовый код
```py
-
```